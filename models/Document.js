class Document{
    constructor(id,ownerId,title,tags,url){
            this.id = id;
            this.ownerId = ownerId;
            this.title=title;
            this.tags=tags,
            this.url=url
    }
}

export default Document